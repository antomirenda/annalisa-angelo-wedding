const openBtn=document.getElementById('openBtn');const envelope=document.getElementById('envelope');const stage=document.getElementById('envelopeStage');const card=document.getElementById('invitationCard');const reveal=document.getElementById('reveal');const muteBtn=document.getElementById('muteBtn');let audioCtx=null,muted=false,opened=false;
function chime(){if(muted)return;try{audioCtx=audioCtx||new (window.AudioContext||window.webkitAudioContext)();const now=audioCtx.currentTime;[523.25,659.25,783.99,1046.5].forEach((f,i)=>{const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type='sine';o.frequency.value=f;o.connect(g);g.connect(audioCtx.destination);g.gain.setValueAtTime(0,now+i*.11);g.gain.linearRampToValueAtTime(.055,now+i*.11+.03);g.gain.exponentialRampToValueAtTime(.001,now+i*.11+.75);o.start(now+i*.11);o.stop(now+i*.11+.8)})}catch(e){}}
function openExperience(){if(opened)return;opened=true;document.getElementById('hero').style.display='none';reveal.scrollIntoView({behavior:'smooth'});setTimeout(()=>{envelope.classList.add('opened');chime();burstPetals(34)},350);setTimeout(()=>{stage.classList.add('hide');card.classList.add('show');burstPetals(48)},1850)}
openBtn.addEventListener('click',openExperience);envelope.addEventListener('click',()=>{if(!opened){openExperience()}else{envelope.classList.add('opened')}});envelope.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openExperience()}});muteBtn.addEventListener('click',()=>{muted=!muted;muteBtn.textContent=muted?'×':'♪'});
function burstPetals(n=20){for(let i=0;i<n;i++){const p=document.createElement('span');p.className='petal';p.style.left=Math.random()*100+'vw';p.style.setProperty('--x',(Math.random()*220-110)+'px');p.style.animationDuration=(5+Math.random()*6)+'s';p.style.animationDelay=(Math.random()*1.2)+'s';p.style.transform=`rotate(${Math.random()*180}deg)`;document.body.appendChild(p);setTimeout(()=>p.remove(),12000)}}setInterval(()=>burstPetals(4),2600);
const target=new Date('2026-09-26T10:30:00+02:00').getTime();function tick(){const d=Math.max(0,target-Date.now());const days=Math.floor(d/86400000);const hours=Math.floor(d%86400000/3600000);const minutes=Math.floor(d%3600000/60000);const seconds=Math.floor(d%60000/1000);document.getElementById('days').textContent=days;document.getElementById('hours').textContent=hours;document.getElementById('minutes').textContent=minutes;document.getElementById('seconds').textContent=seconds}tick();setInterval(tick,1000);
const canvas=document.getElementById('sparkles'),ctx=canvas.getContext('2d');let w,h,particles=[];function resize(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';particles=Array.from({length:Math.min(95,Math.floor(innerWidth/12))},()=>({x:Math.random()*w,y:Math.random()*h,r:(Math.random()*1.6+.4)*devicePixelRatio,a:Math.random(),s:(Math.random()*.25+.05)*devicePixelRatio}))}addEventListener('resize',resize);resize();function draw(){ctx.clearRect(0,0,w,h);for(const p of particles){p.y-=p.s;p.a+=.012;if(p.y<0)p.y=h;ctx.beginPath();ctx.fillStyle=`rgba(247,221,190,${.15+Math.sin(p.a)*.12})`;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)}draw();
